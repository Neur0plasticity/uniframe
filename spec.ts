import { uniframe } from './main';

const alg = uniframe;
console.log(alg);

console.warn(`
    * All modules
    * transpiler support
    * credentials storage
    * Sandbox-js
    * Dependency Injection Management
    * Namespace
    * Params
    * Bootstrap
    * Design Patterns on standby
    * effortless support funC
    * effortless support claS
    * (e2e & segmented) versioning spec testing
    * proper naming sytems like
    --- uil     replaces backend    user interface logic
    --- uiv     replaces frontend   user interface visual
    --- uia     replaces audio      user interface audio
`);
console.warn(`
    filesystem design:
        <miscellaneous files/folders>
        <custom CLI commands>
        <DEPLOY>
        <all modules in one flat folder ... no nestations allowed>
        <spec folder>
        <documentation>
        <entire app is data input only>
`);
(function TODOLIST(list:string[]){
    console.log("TODOLIST");
    console.log(...list);
})([
    "Fix all modules required to import into UNIFRAME",
    "Program project folder structure",
    "Program E2E/Segmented Tester",
    "Program Codel File structure",
    "Program CLI",
    "Program UNIFRAME"
]);